<nav class="nav">
    <a class="nav-logo-link" title="{{ 'app.home.logo.link.label'|trans }}" href="{{ path('app_home') }}"><img src="{{ asset("images/logo.svg") }}" alt="RecetteDay" class="nav-logo"/></a>
    {% if app.user %}
        <a class="nav-link" href="{{ path('app_logout') }}"><i class="fas fa-user"></i> {{ 'app.navbar.logout.label'|trans }}</a>
    {% else %}
        <a class="nav-link" href="{{ path('app_login') }}"><i class="fas fa-user"></i> {{ 'app.navbar.sign_in.label'|trans }}</a>
    {% endif %}
</nav>